Role: You are a Senior SDET and Test Architect.

Task: Generate a single Bash shell script that will completely bootstrap a modern, BDD-driven, scalable API test automation framework. 

Technical Stack & Architecture:
- Core: Java 17+, RestAssured, and Jackson (or Gson) for JSON processing.
- BDD Framework: Cucumber JVM (v7+).
- Test Runner: Strictly use JUnit 4 (@RunWith(Cucumber.class)). Do NOT use TestNG or JUnit 5. Use exactly ONE Runner class.
- Design Pattern: API Service/Controller Pattern and POJOs for Serialization/Deserialization.
- Dependency Injection: Use PicoContainer to share the TestContext (which should hold the Cucumber Scenario, RestAssured Response, and Request payloads) across step definitions.
- Step Definitions: Consolidate all step definitions into a SINGLE `StepDefinitions.java` file.

Project Structure Rules:
- Strictly utilize Maven. 
- ALL source code (POJOs, API Clients, Steps, Runners, Context, Config) MUST reside under `src/test/java`.
- ALL resources (Features, Config properties) MUST reside under `src/test/resources`.

Core Requirements:
- Dynamic Configuration: Create a `ConfigReader` to handle environment variables (Base URI, API keys/tokens) via a `config.properties` file.
- RestAssured Setup: Implement a utility class utilizing `RequestSpecBuilder` and `ResponseSpecBuilder` to establish default BaseURIs, Headers (e.g., Content-Type: application/json), and logging filters.
- Reporting: Strictly use Native Cucumber HTML and JSON reports. Do NOT use Extent Reports or Allure. 
- Hooks & Context (CRITICAL): Inject the Cucumber `Scenario` interface. Use `scenario.log()` inside Step Definitions or Hooks to append API Request details (Method, Endpoint, Payload) and API Response details (Status Code, Response Body) directly into the native report.
- Parallel Execution: Configure the `pom.xml` `maven-surefire-plugin` to execute tests in parallel at the scenario level (`<parallel>methods</parallel>`).
- CI/CD: Include a `main.yml` GitHub Actions workflow that runs the API tests and uploads the native Cucumber HTML report as an artifact.

Sample Implementation Details:
- Target API: reqres.in (https://reqres.in/)
- Feature 1 (GET Request): Retrieve a list of users. Verify the status code is 200. Extract and validate a specific data point from the JSON response.
- Feature 2 (POST Request): Create a new user using a Java POJO for the request body payload. Send the request, verify the status code is 201, and deserialize the response to validate the created data.

Deliverable:
Output NOTHING but a valid Bash script block (
